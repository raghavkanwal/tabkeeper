<%= form_for (@bill) do |f| %>
  <div class="field">
     Amount : <%= f.text_field :amount, :placeholder => "Rs. "%><br/>
     Name   :   <%= f.text_field :for, :placeholder => "Name" %>
  </div>

  <%= f.submit "Post"%>
<% end %>
<%= link_to "Back", user_path(:id => current_user.id)%>
