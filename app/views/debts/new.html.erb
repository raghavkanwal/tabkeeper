<%= form_for (@debt) do |f| %>
  <div class="field">
     Amount : <%= f.text_field :amount, :placeholder => "Rs. "%><br/>
     Name :<%= f.text_field :to, :placeholder => "Name" %>
  </div>

  <%= f.submit "Post"%>
<% end %>
<%= link_to "Back", user_path(:id => current_user.id)%>