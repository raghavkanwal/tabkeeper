<%= form_for (@debt) do |f| %>
  <div class="field">
     Amount : <%= f.text_field :amount, :placeholder => "Rs. "%>
     Name :<%= f.text_field :to, :placeholder => "Name" %>
  </div>

  <%= f.submit "Post"%>
<% end %>
