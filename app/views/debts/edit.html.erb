<h1>Editing debt</h1>

<%= form_for (@debt) do |f| %>
  <div class="field">
     Amount : <%= f.text_field :amount, :placeholder => "Rs. "%>Name :<%= f.text_field :for, :placeholder => "Name" %>
  </div>

  <%= f.submit "Update" %>
<% end %>


<%= link_to 'Show', @debt %> |
<%= link_to 'Back', debts_path %>
